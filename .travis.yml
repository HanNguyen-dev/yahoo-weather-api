language: java

os: linux
dist: bionic
jdk:
  - oraclejdk11

jobs:
  include:
      - stage: compile
        script:
          - cd yahoo-weather-service
          - gradle clean build
      - stage: test
        script:
          - cd yahoo-weather-service
          - gradle test
